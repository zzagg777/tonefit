// import { render, screen } from '@testing-library/react';
// import App from '../App';

// describe('App 컴포넌트', () => {
//   test('텍스트가 화면에 보여야 한다', () => {
//     // App 컴포넌트를 렌더링
//     render(<App />);

//     // 'Tailwind 동작 확인!' 텍스트가 있는지 확인
//     expect(screen.getByText('Tailwind 동작 확인! 🎉')).toBeInTheDocument();
//   });
// });
